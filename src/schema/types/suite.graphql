type Suite {
  _id: ID!
  name: String
  description: String
  requirementId: String
  status: String
  configuration: JSON
  exception: String
  createdAt: String
  updatedAt: String
  execution: Execution
}

type Query {
  suites: [Suite]
}
